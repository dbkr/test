name: testy
on:
  pull_request_target:
    types: [ opened, edited, labeled ]
jobs:
  its_the_test_job:
      runs-on: ubuntu-latest
      steps:
        - uses: matrix-org/allchange@dbkr/checkeraction
          with:
            ghToken: ${{ secrets.GITHUB_TOKEN }}
